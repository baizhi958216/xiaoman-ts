const p05 = () => {
  /*
     // 布尔值数组
    let arr: boolean[] = [true, false];
    //   或者使用泛型
    let arr1: Array<boolean> = [true, false];
    //   定义对象数组
    interface X {
        name: string;
    }
    let arr3: X[] = [{ name: "小满" }, { name: "胡萝卜" }];
    //   定义二维数组
    let arr4: number[][] = [[1], [2], [3]];
    //   定义多维数组
    let arr5: number[][][] = [[[1]], [[2]], [[3]]];
    let arr5_1: Array<Array<Array<number>>> = [[[1]], [[2]], [[3]]];
    //   大杂烩数组
    let arr6: any[] = [1, true, "123", {}];
    // 使用元组
    let arr7: [number, boolean, string, object] = [1, true, "123", {}];
    // 函数
    function a(...args: number[]) {
        // 剩余参数,解构出来就是数组
        console.log(args);
        // 类数组对象, 没有数组的内置方法
        let a: IArguments = arguments;
    }
    a(1, 2, 3, 4, 5); 
    
    */
};
export { p05 };

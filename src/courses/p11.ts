const p11 = () => {
  /* 
    // 元组可以使用他们独有的属性和方法, 确定类型, 数量固定
    const arr: readonly [number, boolean, string] = [1.5, false, "12345"];
    console.log(arr[0].toFixed());
    console.log(arr[1].valueOf());
    console.log(arr[2].toLowerCase());
    // 别名
    const arr1: readonly [x: number, y?: boolean] = [12];
    console.log(arr1[1]);
    // 设置类型别名
    type Person = [string, string, number];
    const excel: readonly Person[] = [
        ["小满", "女", 18],
        ["小满", "女", 18],
        ["小满", "女", 18],
        ["小满", "女", 18],
        ["小满", "女", 18],
    ];
    console.log(excel);
    // 把值变成类型
    type first = typeof arr1[0];
    type second = typeof arr1["length"];
    const num: first | second = 123;
    console.log(num);
 */
};
export { p11 };

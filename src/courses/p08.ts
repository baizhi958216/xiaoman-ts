const p08 = () => {
  /*
     // ecma类型定义
    let num: Number = new Number(1);
    let data: Date = new Date();
    let reg: RegExp = new RegExp(/\w/);
    let error: Error = new Error("错了");
    let xhr: XMLHttpRequest = new XMLHttpRequest(); 
  */
  /*
    //  DOM元素获取
    //  (HTML元素名称)Element
    let div: HTMLInputElement = document.querySelector("input");
    //   一些不知道的断言成HTMLElement/Element
    let footer = document.querySelector("footer") as Element;
    //   DOM节点获取
    let divNode: NodeList = document.querySelectorAll("div");
    divNode.forEach;
    //   如果是动态的
    let dynamicNode: NodeListOf<HTMLDivElement | HTMLElement> =
        document.querySelectorAll("div");
    dynamicNode.forEach; 
  */
  /*
    // BOM元素获取
    let local: Storage = localStorage;
    let lo: Location = location;
    let promise: Promise<string> = new Promise((r) => r("小满"));
    promise.then(res=>res.split) 
  */
};
export { p08 };

const p10 = () => {
  /*
    // 抽象类: 里面只能描述, 不能进行实现, 也不能实例化
    abstract class Vue {
        name: string;
        constructor(name?: string) {
        this.name = name;
        }
        getName(): string {
        return this.name;
        }
        abstract init(name: string): void;
    }
    //   派生类
    //   继承抽象类需要把抽象类的方法实现
    class React extends Vue {
        constructor() {
        super();
        }
        init(name: string): void {}
        setName(name: string) {
        this.name = name;
        }
    }
    const react = new React();
    react.setName("小满-丝袜");
    console.log(react.getName()); 
  */
};
export { p10 };
